---
phase: 02-design
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-design/02-DESIGN-SPEC.md
autonomous: false
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
  - SUM-01
  - SUM-02
  - SUM-03
  - MAC-01
  - NAV-01

must_haves:
  truths:
    - "A design specification exists that precisely defines the statistics page layout at 375px"
    - "Bar chart section specifies 7 bars (Mo-So) with color rules (green/red/gray) and dashed goal reference line"
    - "Summary cards section specifies 2+1 grid with Kalorien, Defizit, and Sport cards using DeficitCard pattern"
    - "Macro section specifies 3-column grid with Protein/KH/Fett as % of daily goal"
    - "Week navigation specifies sticky bar with KW format, prev/next arrows, conditional Heute button"
    - "All colors reference existing CSS variable tokens — no custom hex values"
    - "A Penpot screen in wfp_v3.pen implements the specification (human-verified)"
  artifacts:
    - path: ".planning/phases/02-design/02-DESIGN-SPEC.md"
      provides: "Pixel-precise design specification for statistics page"
      contains: "375px"
  key_links:
    - from: "02-DESIGN-SPEC.md"
      to: "wfp_v3.pen"
      via: "Human creates Penpot screen from spec"
      pattern: "specification"
---

<objective>
Create the visual design specification for the weekly statistics page and build the design in Penpot.

Purpose: Phase 3 needs a pixel-precise design reference to implement UI components. All design decisions are locked from the discussion phase — this plan translates them into a buildable specification and then into a Penpot screen.

Output: `02-DESIGN-SPEC.md` (machine-precise spec) and a completed Penpot screen in `wfp_v3.pen`.
</objective>

<execution_context>
@/Users/sebastianpieper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastianpieper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design/02-CONTEXT.md
@.planning/phases/02-design/02-RESEARCH.md

Key data types from Phase 1 (via 01-01-SUMMARY.md):
- `WeeklyStats`: 7 `DayStats` entries, `goals`, totals, `loggedDayCount`, `avgEatenCalories`
- `DayStats`: `hasData`, `eatenCalories`, `sportCalories`, `deficit`, macros, `sportSessions`
- `ResolvedGoals`: `targetCalories`, `protein`, `carbs`, `fat` (all `number | null`)

CSS tokens (from 02-RESEARCH.md):
- Success green: `hsl(142, 72%, 37%)`
- Destructive red: `hsl(0, 72%, 51%)`
- Muted gray: `hsl(210, 40%, 96%)`
- Protein blue: `hsl(217, 91%, 60%)`
- Carbs green: `hsl(160, 84%, 39%)`
- Fat red: `hsl(0, 84%, 60%)`
- Card bg: `hsl(0, 0%, 100%)`, border: `hsl(214, 32%, 91%)`, radius: 12px, shadow-soft
- Typography: Label=14px muted, Big number=24px bold tabular-nums, Subtitle=12px muted
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pixel-precise design specification</name>
  <files>.planning/phases/02-design/02-DESIGN-SPEC.md</files>
  <action>
Create a comprehensive design specification document at `.planning/phases/02-design/02-DESIGN-SPEC.md` that translates all locked decisions from 02-CONTEXT.md into a pixel-precise, buildable spec. The spec must be detailed enough for Sebastian to recreate it 1:1 in Penpot without interpretation.

Structure the spec as follows:

**Frame:** "Statistics Page" — 375px wide, ~900px tall, vertical scroll, background `hsl(60, 9%, 98%)`

**Section 1: Week Navigation Bar (sticky, 52px height)**
- Full width, `bg-background`, `border-b border-border`
- Left: chevron-left button (ghost style, 40x40 tap target)
- Center: "KW 11 · 10.--16. Mar" in 16px Inter font-medium
- Right: "Heute" text button (ghost, only shown when NOT current week) + chevron-right button
- Show TWO states: current week (no Heute button) and past week (with Heute button)
- "Heute" positioned between week label and right arrow

**Section 2: Bar Chart (~240px height, padding 16px horizontal)**
- 7 bars for Mo-So, evenly spaced
- Day labels below each bar: "Mo", "Di", "Mi", "Do", "Fr", "Sa", "So" in 12px muted
- Y-axis on left with 3-4 scale labels in 10px muted (e.g., 0, 1000, 2000, 3000)
- Dashed horizontal reference line at goal level with "Ziel: 2,100 kcal" label right-aligned in 11px muted
- Bar color rules:
  - Logged + on/under target: `hsl(142, 72%, 37%)` (success green)
  - Logged + over target: `hsl(0, 72%, 51%)` (destructive red)
  - Not logged (hasData=false): small ~24px gray stub in `hsl(210, 40%, 96%)`
- Sample data for the design (7 days):
  - Mo: 1,850 kcal (green, under target)
  - Di: 2,300 kcal (red, over target)
  - Mi: 1,950 kcal (green, under target)
  - Do: not logged (gray stub)
  - Fr: 2,100 kcal (green, exactly at target)
  - Sa: 2,500 kcal (red, over target)
  - So: 1,700 kcal (green, under target)

**Section 3: Summary Cards (padding 16px)**
- Gap between cards: 12px (gap-3)
- Row 1: 2-column grid
  - Kalorien Card (`.card p-4`, 12px radius, border, shadow-soft):
    - "Kalorien" in 14px muted
    - "12,400" in 24px bold tabular-nums
    - "O 2,067 kcal/Tag" in 12px muted (Ø character)
  - Defizit Card (same card style):
    - "Defizit" in 14px muted
    - "2,200" in 24px bold tabular-nums, colored `hsl(142, 72%, 37%)` (green = deficit is good)
    - "kcal" in 14px normal weight
- Row 2: Full-width Sport Card (same card style):
  - "Sport" in 14px muted
  - "1,230" in 24px bold tabular-nums
  - "kcal · 4 Sessions" in 14px muted

**Section 4: Macro Averages (padding 16px)**
- Optional heading: "O Makros" in 14px muted (Ø character) above the grid
- 3-column grid, gap-4
- Each column: value in 24px bold tabular-nums (colored) + label in 12px muted below
  - Protein: "87%" in `hsl(217, 91%, 60%)`, label "Protein"
  - Kohlenhydrate: "102%" in `hsl(160, 84%, 39%)`, label "Kohlenhydrate"
  - Fett: "94%" in `hsl(0, 84%, 60%)`, label "Fett"

Include a "Data Mapping" section that maps each visual element to its `WeeklyStats` field (as documented in 02-RESEARCH.md Pitfall 5).

Include the complete CSS variable reference table from 02-RESEARCH.md so Sebastian has all color values in one document.

Do NOT include loading/skeleton states — keep the spec focused on the primary state. Skeleton states are Claude's discretion for Phase 3.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-design/02-DESIGN-SPEC.md && grep -q "375px" .planning/phases/02-design/02-DESIGN-SPEC.md && grep -q "hsl(142" .planning/phases/02-design/02-DESIGN-SPEC.md && grep -q "WeeklyStats" .planning/phases/02-design/02-DESIGN-SPEC.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Design spec file exists with complete section definitions, sample data, exact color tokens, typography specs, and data field mappings. No ambiguity remains for Penpot construction.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Build Penpot design screen from specification</name>
  <action>
Sebastian creates the statistics page screen in Penpot using the design specification from Task 1.

**Open:** `/Users/sebastianpieper/Development/wfp_v3.pen` in Penpot
**Create:** New frame "Statistics Page" at 375px width
**Build:** Each section following `02-DESIGN-SPEC.md` exactly — Week Nav, Bar Chart, Summary Cards, Macro Averages
**Use:** Existing styleguide components/colors from the wfp_v3.pen file
  </action>
  <how-to-verify>
After building the Penpot screen, verify against this checklist:

1. Frame is 375px wide
2. Week Navigation: sticky bar with "KW XX" format, prev/next arrows, conditional "Heute" button (show past-week state)
3. Bar Chart: 7 bars (Mo-So), mix of green/red/gray bars visible, dashed goal reference line with "Ziel: X kcal" label
4. Summary Cards: 2+1 grid — Kalorien + Defizit side by side, Sport full width below
5. Macro Averages: 3-column grid with Protein/KH/Fett percentages in correct colors
6. All colors use existing styleguide tokens (no custom hex values)
7. Typography matches existing patterns (14px labels, 24px numbers, 12px subtitles)
  </how-to-verify>
  <resume-signal>Type "design complete" when the Penpot screen is finished, or describe any issues/deviations</resume-signal>
  <done>A complete statistics page design exists in wfp_v3.pen showing all sections (Week Nav, Bar Chart, Summary Cards, Macro Averages) at 375px width using the existing styleguide.</done>
</task>

</tasks>

<verification>
Phase 2 verification is entirely visual/manual:
1. Design spec document exists and is comprehensive
2. Penpot screen exists in wfp_v3.pen
3. All 8 requirements (VIS-01 through NAV-01) are visually represented in the design
4. Design uses only existing styleguide tokens
</verification>

<success_criteria>
- 02-DESIGN-SPEC.md exists with pixel-precise specifications for all 4 page sections
- A Penpot screen in wfp_v3.pen shows the complete statistics page at 375px
- Design contains: week navigation, bar chart with goal line, 3 summary cards (2+1 grid), macro averages (3-col grid)
- Unlogged days visible as gray placeholder bars in chart
- All colors reference CSS variable tokens from the existing codebase
</success_criteria>

<output>
After completion, create `.planning/phases/02-design/02-01-SUMMARY.md`
</output>
